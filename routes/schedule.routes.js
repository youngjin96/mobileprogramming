module.exports = app =>{
    const schedule = require("../controllers/schedule.controller.js");

    //app.get("/schedule", schedule.createScheduleTable);//캘린더 테이블 만들기
    //app.get("/schedule", schedule.scheduleSerch);//allserch
    //app.post("/schedule", schedule.createSchedule); // insert
    //app.get("/schedule", schedule.checkSchedule);//schedule serch (id기준 날짜보내주기)
    //app.delete("/schedule", schedule.deleteSchedule); //delete 
    //http://localhost:5000/schedule
};